# notebooks/

Exploration only. Nothing here is load-bearing for pipeline results —
anything that affects results (QC thresholds, statistical models, scoring
logic) lives in `src/oncocartograph/` with tests, not here.

Notebooks in this directory may become stale as the pipeline evolves and
are not run as part of CI.
